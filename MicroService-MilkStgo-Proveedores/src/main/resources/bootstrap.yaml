spring:
  application:
    name: MicroService-MilkStgo-Proveedores
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    plataform: postgres
    driver-class-name: org.postgresql.Driver
    dbname: MilkStgo_Proveedores
    url: jdbc:postgresql://localhost:5432/MilkStgo_MicroService_Proveedores
    username: postgres
    password: Simon_789

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'