eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://backend-eureka-service:8761/eureka/
spring:
  application:
    name: Gateway
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081